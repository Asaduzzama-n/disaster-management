/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['thumbor.forbes.com', 'res.cloudinary.com', 'images5.alphacoders.com']
    }
};

export default nextConfig;
